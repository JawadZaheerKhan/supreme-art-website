{
  "name": "supreme-art-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 3000 --directory dist"
  }
}
